% layout 'default', title => 'Processing file';
<h5>Working on the file</h5>
<p>
Found mail with subject: <b><%= $subj %></b>
% if( $res ) {
    <pre>
      %= dumper( attachments => $attachments )
      %= dumper( res => $res )
    </pre>
% }
<div class="debug">
  %= dumper( action => $action )
</div>
</p>
