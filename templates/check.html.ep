Found mails:
      <ul>
        % foreach my $message ( @$messages ) {
        <li><%= $message->{subj} %></li>
        <ul>
          % foreach my $attachment( @{$message->{attachments}} ) {
          %     if( $attachment->{filename} =~ m/pdf$/ ) {
          <li><%= link_to $attachment->{filename} => "/work/fetch?mess=$message->{id}&attach=$attachment->{id}" %></li>
          %     }
          %     else {
          <li><%= $attachment->{filename} %></li>
          %     }
          % }
        </ul>
        % }
      </ul>
