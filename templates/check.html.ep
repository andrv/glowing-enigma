Found mails:
      <ul>
        % foreach my $message ( @$messages ) {
        <li><%= $message->{subj} %></li>
        <ul>
          % foreach my $attachment( @{$message->{attachments}} ) {
          <li><%= link_to $attachment->{filename} => "/work/fetch/$message->{id}/$attachment->{id}" %></li>
          % }
        </ul>
        % }
      </ul>
