Found mails:
      <ul>
        % foreach my $message ( @$messages ) {
        <li><%= $message->{subj} %></li>
        <ul>
          % foreach my $attachment( @{$message->{attachments}} ) {
          %     if( $attachment->{filename} =~ m/jpg$|png$|pdf$/ ) {
          <li><%= link_to $attachment->{filename} => "/fetch/$message->{id}/$attachment->{id}/$attachment->{filename}" %></li>
          %     }
          %     else {
          <li><%= $attachment->{filename} %></li>
          %     }
          % }
        </ul>
        % }
      </ul>
