% layout 'default', title => 'Processing file';
<h5>Working on the file</h5>
    <p>
      % if( $messagesFound ) {
      %=    include 'check', messages => $messages;
      % }
      % else {
      Nothing to do
      % }
      <pre>
      </pre>
      <div class="debug">
      </div>
    </p>
