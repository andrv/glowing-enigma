{
    # put client secret json here
    appSecrets => 'credentials/client_secret.json',

    # source files store
    sourceFiles => 'sources',

    # processed files store
    targetFiles => 'targets',
}
